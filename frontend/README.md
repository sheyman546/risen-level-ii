# Frontend — Multi-Wallet Payment Tracker

React + TypeScript + Vite frontend for the payment tracker dApp. See the
[root README](../README.md) for full setup, deployment, and architecture docs.

```bash
npm install
npm run dev     # start dev server
npm run build   # typecheck + production build
```
